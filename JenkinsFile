pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script {
                    echo "script execution started"
                }
            }
        }
    }
}
