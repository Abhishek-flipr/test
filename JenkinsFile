pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script {
                    sh "python test.py"
                    echo "script execution started"
                }
            }
        }
    }
}
